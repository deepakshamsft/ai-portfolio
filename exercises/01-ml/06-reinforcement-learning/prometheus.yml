# Prometheus configuration for AgentAI monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'agentai-api'
    static_configs:
      - targets: ['agentai-api:5000']
    metrics_path: '/metrics'
