# Prometheus configuration for FraudShield

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'fraudshield-api'
    static_configs:
      - targets: ['fraudshield-api:5000']
    metrics_path: '/metrics'
