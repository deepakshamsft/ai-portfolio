# Prometheus configuration for FaceAI

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'faceai-api'
    static_configs:
      - targets: ['faceai-api:5000']
    metrics_path: '/metrics'
